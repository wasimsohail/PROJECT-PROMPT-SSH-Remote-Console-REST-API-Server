# PROJECT-PROMPT-SSH-Remote-Console-REST-API-Server
Create a standalone Multi OS (windows Linux) Python application that provides a REST API interface for managing SSH connections and executing commands on remote systems. This will be a pure API server with no GUI - all interactions happen through HTTP REST endpoints.
